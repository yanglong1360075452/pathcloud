## mysql jdbc config
dataSource.driverClassName=${pom.jdbc.driver}
dataSource.url=${pom.jdbc.url}
dataSource.username=${pom.jdbc.username}
dataSource.password=${pom.jdbc.password}

# For hikari db connect pool
dataSource.pool.max_size=100